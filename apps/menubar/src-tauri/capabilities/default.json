{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Focusa menubar core, signed updater, and controlled relaunch permissions.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "updater:default",
    "process:allow-restart"
  ]
}
