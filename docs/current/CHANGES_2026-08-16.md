# Change log — 2026-08-16 closure session (for future agents)

Why this file: every change this session is listed with its REASON so
future agents can reconstruct intent without archaeology. Read
docs/161-2026-08-15-bug-closure-and-operations-hardening-session-record.md
for the chronological narrative; this is the index.

## Operator directives that shaped the work

1. TBQ (terminal never blocks) — all long work must be backgrounded and
   notify the agent's front terminal on completion.
2. NO handoffs; continuous asynchronous closure; multitask.
3. Multi-agent work = silent-session orchestration, workloop-compatible
   — NOT a new orchestrator.
4. Fast-forward multiplier (2x/4x/6x/8x) — operator-conceived on the
   fly, formalized as #312, bound to the CallGraph spec vocabulary.
5. Everything documented on every surface; nothing half-baked.
6. Security + performance improvements first.
7. E2E-verify the entire ledger + all features before release.

## Features landed (what + why)

### focusa bg — background execution primitive (#311-family)
- WHAT: `focusa bg run/status/wait/list` + daemon background_jobs ledger
  + completion envelope with bounded output_tail (4KB) + SSE broadcast +
  Pi front-terminal delivery (notify + appendEntry) + monitor-lost
  reaping + idempotent settlement.
- WHY: TBQ directive — agents needed a durable, notifying background
  primitive; raw shell wrappers were brittle and silent.

### bg tools (Pi extension)
- focusa_bg_run / focusa_bg_run_many (parallel orchestration) /
  focusa_bg_status. Strict schemas. WHY: agents reach typed primitives
  through tool discovery, not shell knowledge.

### Runtime Constitution (#256 slice 1)
- WHAT: canonical hash-bound behavioral law in focusa-core, served by
  GET /v1/runtime-constitution; extension activates the served artifact;
  inline law demoted to offline fallback.
- WHY: per-harness hard-coded prompt law was drift; one versioned
  artifact with digest verification.

### CallGraph authority (#254 slices 1-10)
- types/validation/eligibility/ledger/dispatch commit boundary/HTTP/
  leases/liveness/replay/routing/events + adapter registry + dispatch
  routing. WHY: the P0 program's foundation; durable-before-dispatch is
  the safety invariant.

### Export + envelopes + team routing (#287/#289/#292)
- One CallGraphExportProjection → JSONL/TODO.txt/DOT/CSV/TSV/Mermaid;
  governed import preview; canonical 9-layer ItemEnvelope; route_frame/
  route_frame_team. WHY: portability surfaces + one-item-one-envelope +
  deterministic failover.

### Direction Workbench (#291)
- typed steer/adjudicate/review + append-only ledger + API. WHY:
  operator steering/proof adjudication as typed, evidence-bound ops.

### Procedure compiler (#298)
- typed procedures + deterministic plans + effect-class confirmation
  gates + reverse rollback. WHY: governed ops with mandatory
  confirmation for destructive/financial/security steps.

### Infrastructure inventory + adoption (#255)
- read-only 17-concern scan + preview-only adoption plan + CLI. WHY:
  adopt-first discipline — never duplicate existing project systems.

### Error envelope (#261)
- canonical FocusaErrorEnvelope + parity audit script; all legacy bare
  errors migrated (0 legacy). WHY: one failure shape across surfaces.

### Acceptance atoms + completion authority + capability truth
(#278/#276/#277/#279)
- typed atoms + deterministic verdicts + evaluate route + closure
  validation gating + public-safe HonestyManifest. WHY: completion truth
  — no false closures.

### Rehydration/continuation (#262/#263)
- ContinuationHandle + partition-path sanitization + fail-closed
  snapshot load + rebuild-state + legacy waypoint compat. WHY: the
  activation window exposed a real data-loss bug; recovery is now a
  first-class tool.

### Fast-forward (#312)
- FanoutPlan (orchestrator lane kind=agent/strong refs + worker lanes
  kind=tool/weak refs, round-robin, per-lane budgets, wait-for-all) +
  route + tool + docs. WHY: operator's GUI vision; CallGraph-bound.

### Runtime bundles + skills + taxonomy + surface audits
(#257/#258/#259/#264)
- RuntimeBundleManifest; 116-tool taxonomy audit (zero real dups);
  skills-manifest with owners; Pi surface audit. WHY: packaging +
  ownership + no screen trash.

### Recovery hardening (#313)
- idempotent bg settlement; bounded adapter registration; single-
  connection bg wait. WHY: security/perf directive.

### Workset program (#267-#274, operator-authorized)

Spec 149 said "implementation not authorized" — the operator directive
of 2026-08-16 ("FINISH. THEN WORKSET") supersedes it, recorded in
docs/current/WORKSET_IMPLEMENTATION_STATE_2026-08-16.md. Authority
separation honored: Workset = membership/disposition/completion
contract + immutable history; CallGraph owns execution. All seven
children landed: #268 freshness stamps, #269 ledger+store, #270
provider reconciliation, #271 API/CLI/Pi tool, #272 bounded context
packets, #273 Mission Canvas surface, #274 evidence-gated transition
DAG. All checks green.

### Deslop duplication gate (new CI)
- .deslop.toml (20% ceiling, fixtures hidden) + advisory workflow +
  AGENTS.md recipe. WHY: operator asked; MIT/free; structural dedup
  converges the remaining boilerplate.

## E2E gates (what + why)

- scripts/e2e-live-route-matrix.mjs: 18/18 live checks against the
  daemon (every bug fix + feature surface). Wired into the release gate.
- scripts/e2e-workspace-gate.sh: full workspace tests, pipefail-safe
  (a false-green bug in the first ad-hoc chain was caught + fixed).
- WHY: the operator required full E2E proof before the next release.

## Deploy/build changes

- .cargo/config.toml: musl static builds + sqlite-vec u_int*_t shim.
  WHY: the OVH build host's glibc 2.39 binaries didn't run on
  AlmaLinux 8 (2.28); static-pie matches the prior deployed binary.
- Deployed daemon + CLI are static musl builds of 0.9.121-dev.
- Live DB state restored from the pre-retention backup + post-22:34
  events (version 6277160; 486 frames, 32 workpoints, 32 trajectories).

## Sync state (for the next agent)

- Everything pushed to origin/local/work-loop-completion.
- Canon big-squash onto main is DEFERRED per operator ("not until we
  close more issues"). Main is 1180 commits ahead (Spec172 line +
  letta-adapter). Do NOT push/squash to main without the operator.
- Open: #288 (operator-Mac exporter), #107 (letta adapter cross-line),
  #128 (PR #129 review), #125 live acceptance, Workset #267/#269,
  #299 credential authority.

### Credential authority (#299 slice 1-2)

Secret-free typed contracts (docs/156): provider descriptors with a
redaction guard (no account ids/secret values in model projections),
credential roles, requirements, use grants, the deterministic grant
verdict (role+operation+exposure+target+consumer match, unexpired,
remaining count — no partial satisfaction), and the grant lifecycle
state machine. No secret material in the models by construction.
