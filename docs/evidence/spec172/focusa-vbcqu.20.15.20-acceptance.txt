Spec 172.03.03 no-license family inheritance and Operator defaults acceptance record
Work item: focusa-vbcqu.20.15.20
Title: Enforce no-license family allowlist and Operator inheritance defaults

Implementation commit: 2fa95680

Exact modified surfaces
- crates/focusa-license/src/entitlement_policy.rs
- crates/focusa-license/src/entitlement_policy_tests.rs
- crates/focusa-license/src/lib.rs
- scripts/generate-spec135-operation-contracts.py
- docs/contracts/spec135/generated-contract-v1/operation-registry.json
- crates/focusa-license/tests/spec172_family_inheritance.rs
- tests/spec172_family_inheritance_test.py

Bounded result
- Added deterministic Spec 172 verified-no-license family classifier:
  `is_focusa_verified_no_license_family_allowed(product, family, mutable_project_count)`.
- Added explicit Focusa/UIAI allowlist and blocked-family constants from
  `docs/contracts/spec172-verified-limited-access.v1.yaml` with fail-closed
  defaults (`unknown`, `new`, and `unknown product` deny).
- Enforced manual_workpoint/mutation boundaries:
  Focusa `manual_project` is allowed only when `mutable_project_count <= 1`;
  unknown families and blocked families are denied; unknown products are denied.
- Added and updated unit/integration tests covering:
  - Focusa allowlist inheritance + closed default behavior.
  - UIAI product boundary and blocked/unknown-family behavior.
  - Operation-registry coverage of allowed Spec 172 families and closed defaults.
- Extended generator to annotate operation metadata with `spec172_family`
  deterministically from operation family taxonomy.

Exact verification (exit codes)
1) `python3 tests/spec172_family_inheritance_test.py` — exit 0
   - Output: `Spec172 family inheritance gate passed`
   - focusa_allowed=6
   - uiai_allowed=6
   - operation_count=108
   - spec172_family_coverage=108
2) `cargo test -p focusa-license spec172_family_inheritance` — exit 0
   - 3 tests passed:
     - `spec172_family_inheritance_classifier_focusa_is_allowlist_driven_and_closed`
     - `spec172_family_inheritance_classifier_uiai_is_product_specific`
     - `spec172_family_inheritance_registry_is_fail_closed_default`
3) `cargo test -p focusa-license` — exit 0
   - 46 tests passed in `src/lib.rs`, 11 in `tests/authority_lease.rs`, 2 in
     `tests/entitlement_policy_grid.rs`, 3 in `tests/spec172_family_inheritance.rs`.

Regression checks (exit codes)
- `python3 tests/spec172_family_inheritance_test.py` and
  `cargo test -p focusa-license spec172_family_inheritance` both required by
  atom and now green, demonstrating inherited and fail-closed behavior for
  materially new/unknown product/family vectors.

Tests added
- crates/focusa-license/src/entitlement_policy_tests.rs: focusa and uiai family
  inheritance unit vectors
- crates/focusa-license/tests/spec172_family_inheritance.rs: three integration
  vectors for allowlist inheritance and fail-closed defaults
- tests/spec172_family_inheritance_test.py: contract-wide gate for limited-access
  and lifecycle defaults plus generator drift check

Rollback
- Revert only this implementation commit `2fa95680` and this evidence record to
  remove this atom's behavior. Preserve customers, orders, refunds, keys,
  licenses, nodes, sequences, customer data, migration journals, and accepted
  authority/evidence state.

No push, deploy, release, merge, or Beads mutation was performed.
