Spec 152F / Spec 172 overlay acceptance record
Work item: focusa-vbcqu.20.14.9
Title: 152F.01.05 Implement recovery, read, export, and security-maintenance allowances
Implementation commit: 85f497d03871bf2701bd43ed3fc34495a0363d26

Exact surfaces
- crates/focusa-license/src/entitlement_policy.rs
- crates/focusa-api/src/middleware/entitlement.rs
- docs/contracts/spec152f-entitlement-policy.v1.yaml

Bounded result
- Added security-prerequisite metadata and family mapping to recovery allowances in
  `crates/focusa-license/src/entitlement_policy.rs`:
  - New `SecurityPrerequisite` enum.
  - New `RecoveryAllowance::security_prerequisites()`.
  - New `RecoveryAllowance::implied_family()` for runtime mapping.
  - Targeted policy tests for typed prerequisite coverage and expected families.
- Implemented route-level recovery/read/export/security-maintenance allowlist behavior in
  `crates/focusa-api/src/middleware/entitlement.rs`:
  - Added recovery allowances for `POST /v1/update/apply`, `POST /v1/export/run`,
    `POST /v1/project/bootstrap/repair`, and `POST /v1/update/rollback`.
  - These paths now evaluate `reduce_entitlement_state` using policy state
    (`PendingUnverified`, `VerifiedNoLicense`, `ActivePaid`, `OfflineGrace`,
    `Expired/RefundedOrRevoked/MissingOrCorrupt`) and short-circuit entitlement
    feature/limit checks when recovery conditions are satisfied.
  - `reserve_route_limit(...)` is skipped for recovery-allowance routes.
- Updated `docs/contracts/spec152f-entitlement-policy.v1.yaml` recovery exception
  details for stability/export/security-maintenance coverage and node-deactivation
  wording.

Exact verification (exit codes)
1. `cargo test -p focusa-license recovery_allowances` — exit 0.
   Result: 3 passed; 0 failed; 0 ignored; 0 measured; 32 filtered out.
2. `cargo test -p focusa-api recovery_allowances` — exit 0.
   Result: 1 passed; 0 failed; 0 ignored; 0 measured; 467 filtered out.

Regression checks (exit codes)
- `cargo test -p focusa-license --test entitlement_policy_grid state_grid` — exit 0.
- `python3 tests/spec152_rest_entitlement_gate_test.py` — exit 0.
- `python3 tests/spec152_recovery_only_route_gate_test.py` — exit 0.

Rollback
- Revert this commit (and this evidence record if required). Preserve all runtime and
  customer-facing state. No push, deploy, release, merge, or Beads mutation was
  performed.
