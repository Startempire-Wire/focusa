Spec 152E acceptance record
Work item: focusa-vbcqu.20.13.53
Title: 152E.06.05 Cut over facades and disable direct install-site issuance
Implementation commit: `48ff8723` (code + fixture + test)
Spec authority: docs/152e-edd-centered-universal-multi-surface-licensing-and-
branded-facade-addendum.md (§2 mandatory decisions 3, 5, 11, 13; §9 facade
registry; §10 public activation API; §11 EDD checkout; §12 Evaluation
forbidden; §21 surface consolidation; §22.3 cutover steps 7-10; §22.4
rollback; §23 acceptance "Website paid Focusa", "Evaluation", "Facade spoof",
and "Legacy install-site record" rows); Specs 152, 150A, 152A-D; Spec 158
implementation excluded. Locked-release correction atom in the spec152f/spec172
async pipeline.

Exact surfaces (this atom only)
- Cutover schema/service:
  docs/contracts/spec152e-authority-cutover.v1.php
  (FocusaSpec152eAuthorityCutoverSchema — wpuiai_cutover_state,
  wpuiai_cutover_state_journal, wpuiai_cutover_denials,
  wpuiai_cutover_legacy_tables, wpuiai_cutover_recovery_surfaces, schema
  migrations/events; FocusaSpec152eAuthorityCutoverService — publishCutoverState,
  cutoverState, stateDigest, routeDisposition, denyInstallSiteIssuance,
  denyDirectStripeFlow, denySelfEvaluation, legacyTableReadOnlyGate,
  retainLegacyValidationRecovery, facadeProxyGate, journalChainValid)
- Cutover state fixture:
  docs/contracts/spec152e-authority-cutover-fixture.v1.json
  (redacted, deterministic; no raw email, raw key, payment id, or secret)
- tests/spec152e_authority_cutover_test.py (exact verification)

Bounded result
- Added the authority cutover gate, the single feature-gated switch for
  Spec 152E §22.3 steps 7-10. publishCutoverState() publishes the exact
  cutover state exactly once (idempotent replay returns the stored state with
  zero writes; any different payload fails closed with
  CUTOVER_STATE_ALREADY_PUBLISHED). The published state asserts
  new_issuance=edd_authority_only, facade_role=presenter_and_bounded_proxy_only,
  spec158=excluded, and issuance requirements verified_registration /
  edd_order_bound / no_local_issuance. Before publication every route, table,
  and surface fails closed with CUTOVER_STATE_REQUIRED — nothing is issuable,
  mutable, or readable as authority.
- Denied direct issuance (Spec 152E §2.11, §12, §22.3 step 8): the install-site
  create / payment-intent / Stripe webhook issuance routes and the
  caller-parameter WPUIAI.com custom issue route each resolve to
  INSTALL_SITE_ISSUANCE_DISABLED with next action use_edd_authority_checkout;
  direct Stripe product flow is denied with STRIPE_DIRECT_FLOW_DENIED (next
  action use_edd_checkout); local self-Evaluation (--eval) is denied with
  LOCAL_EVALUATION_DENIED (next action use_edd_evaluation). Every denial is
  audited exactly once per (surface, request_id, idempotency_key) tuple in
  wpuiai_cutover_denials and appended to the digest-chained journal; denial
  replays return the stored result with zero new rows.
- Legacy tables read-only (Spec 152E §22.3 step 10): wpuiai_licenses and
  wpuiai_license_audit are registered read-only; legacyTableReadOnlyGate()
  permits SELECT only (bounded validation/recovery) and fails closed with
  LEGACY_TABLE_READ_ONLY for INSERT/UPDATE/DELETE/REPLACE/ALTER and for any
  unregistered table — nothing new is written into legacy truth.
- Bounded legacy validation/recovery retained (Spec 152E §18, §22.3 step 9):
  the nine retained surfaces (legacy validate/keys-validate/status and
  recovery status/export/diagnostics/repair/update/uninstall) are registered
  with grants_entitlement=false and resolve through retainLegacyValidationRecovery;
  entitlement-granting surfaces are never retained.
- Facade proxy only (Spec 152E §2.3, §9, §10): facadeProxyGate() resolves
  every one of the eleven registered facade activation surfaces to its exact
  EDD authority kernel route (matching the facade registry proxy_routes) and
  there is no local issuance action on the facade; routeDisposition() classifies
  legacy install-site routes as denied_issuance / proxy_to_authority /
  legacy_read_only and fails closed with FACADE_ROUTE_DENIED for anything else.
- All registries are validated against server-owned allowlists (DENIED_SURFACES,
  LEGACY_TABLES, RETAINED_SURFACES, FACADE_ACTIONS, INSTALL_SITE_PROXY_ACTIONS);
  denial codes and next actions come from the contract, never from the caller.
  The decision signature has no price, grant, tier, limit, or feature inputs.
- Rollback is preservation-only: preserveForRollback() journals
  rollback_preserved; no DELETE/TRUNCATE/DROP path exists anywhere in the
  contract; state, journal, denials, and registries are never deleted, and EDD
  customer/order/refund truth, verified identities, licenses, nodes, sequences,
  credentials, Workpoints, Evidence, and migration journals are preserved.
- No unverified-email promotion, no local/self-issued entitlement, no
  independent facade authority, no client-controlled EDD price/grants, and no
  secret or unmasked real-email evidence anywhere in the contract, fixture, or
  test output. Cargo/release builds remain deferred until the operator's 50%
  gate; this atom touches no Rust surface, so no cargo build was run.

Exact verification (exit codes)
1. `python3 tests/spec152e_authority_cutover_test.py` — exit 0 (run three
   times; deterministic, byte-identical harness output; harness sha256
   af759c0d4f7001115c8efeb87d707f788be959f0974171e340ae60b5b2508e48).
   Validation summary:
   fixture_sha256=625be0b1f5f9f3e6cfc3fcc0b01c463e72b061046d6c7986665c2b8dae6d09f1
   contract_sha256=67c196d4724417e9bccca18ccdf92081555b275e04e83254d274becda218ebf1
   positive_checks=87  negative_checks=23  state_rows=1  journal_entries=7
   denial_rows=6  legacy_registry_rows=2  recovery_registry_rows=9
   publish_replay_second_state_row=0  denial_replay_second_row=0
   rollback_preserved=1  journal_chain_valid=true  result=passed
   The gate statically validates the fixture (exact denied issuance surfaces,
   read-only legacy tables, retained recovery surfaces, facade proxy == EDD
   authority endpoints, redaction) and the contract (server-owned allowlists,
   fail-closed codes, SELECT-only legacy operations, replay-safe journal chain,
   no destructive statements, no raw email/price/grant inputs), then executes
   the PHP adapter twice against sqlite with byte-identical output: pre-publish
   fail-closed gates, one immutable publication, idempotent replay, six audited
   denials, idempotent denial replay, SELECT-only legacy gate with all mutation
   denials, nine retained recovery surfaces, eleven facade proxy resolutions,
   full journal chain validation, and preservation-only rollback.

Regression checks (exit codes)
- `python3 tests/spec152e_paid_record_migration_test.py` — exit 0 (prior atom
  20.13.50 migration contract untouched).
- `php tests/spec152e_key_quarantine_test.php` — exit 0 (prior atom 20.13.52:
  positive=251, negative=15).
- `php tests/spec152e_legacy_customer_migration_test.php` — exit 0 (prior atom
  20.13.51: positive=151, negative=18).
- `python3 tests/spec152e_api_contract_test.py` — exit 0 (generated activation
  contracts remain current; this atom adds a standalone contract and does not
  touch the activation call-stack source/generator).
- `python3 tests/spec152e_migration_inventory_test.py` — exit 0 (prior atom
  20.13.49 inventory contract untouched; physical_records=596).
- `python3 tests/spec152e_recovery_only_surface_test.py` — exit 0 (recovery
  posture contract intact).
- `python3 tests/spec152e_facade_registry_contract_test.py` — exit 0 (facade
  registry intact; cutover facade proxy routes match its proxy_routes).
- `python3 tests/spec152e_presenter_parity_test.py` — exit 0 (presenter parity
  matrix intact).
- `php tests/spec152e_install_facade_test.php` — exit 0 (install facade
  installers/asset manifest intact).
- `python3 tests/spec152e_install_facade_routes_test.py` — exit 1:
  "repository digest drift for installer.unix". This failure is PRE-EXISTING
  and unrelated to this atom: it reproduces byte-identically on the pristine
  base (documented in atom 20.13.47's acceptance record) and no file this atom
  adds (spec152e-authority-cutover.v1.php, the fixture, and the gate test)
  participates in that installer generator/drift check.
- `php -l docs/contracts/spec152e-authority-cutover.v1.php` — exit 0 (no
  syntax errors).
- No cargo command was run: this atom touches no Rust surface and the
  locked-release FORBIDDEN rule defers cargo/release builds to the operator's
  50% gate.

Tests added
- tests/spec152e_authority_cutover_test.py — the exact verification gate
  (87 positive / 23 negative checks): fixture structure and redaction; denied
  issuance surfaces and exact fail-closed codes; SELECT-only legacy table gate;
  retained recovery surfaces with grants_entitlement=false; facade proxy == EDD
  authority endpoints; static contract invariants (server-owned allowlists,
  replay-safe journal chain, preservation-only rollback, no destructive
  statements, no raw email/price/grant inputs); and behavioral
  publish/replay/immutability/denial/read-only/proxy/rollback execution of the
  PHP adapter with byte-identical replayable output.

Rollback
- Revert only this atom's commits (implementation commit `48ff8723` plus the
  evidence commit containing this file). Preserve EDD customer/order/refund
  truth, verified identities, licenses, nodes, sequences, credentials,
  Workpoints, Evidence, and migration journals; cutover publication/denial
  journal truth is preservation-only and no authority, EDD, node, lease, or
  local install state is mutated by these proofs and replay tests.

No push, deploy, release, merge, or Beads mutation was performed.
