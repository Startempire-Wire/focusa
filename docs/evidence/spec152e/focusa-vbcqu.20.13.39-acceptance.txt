Spec 152E shared activation client and presenter-neutral reducer — acceptance evidence
Work item: focusa-vbcqu.20.13.39
Title: 152E.05.01 Implement shared activation client and presenter-neutral reducer
Scope: crates/focusa-license authority client/HTTP/credentials; shared
activation state reducer; typed facade operations/errors
Spec authority: docs/152e-edd-centered-universal-multi-surface-licensing-and-
branded-facade-addendum.md (§5 universal registration state machine, §6
verified identity and customer promotion, §9 branded facade registry, §10
public activation API, §12 Evaluation journey with the Spec 172 overlay, §14
terminal/agent/source-build experience, §16 dual-channel license delivery,
§18 refund/revoke/recovery, §20 stable failure semantics, §21 surface
consolidation, §24 completion gate); frozen contracts
docs/contracts/spec152e-activation-internal.v1.json,
spec152e-activation-errors.v1.json, spec152e-activation-public-openapi.v1.json,
spec152e-activation-call-stack.v1.yaml; Specs 152, 150A, 152A-D; Spec 158
implementation excluded.

Exact surfaces (this atom only)
- crates/focusa-license/src/activation_reducer.rs (new): presenter-neutral
  registration state reducer — 19 states, 18 typed events, exactly the 48
  frozen transitions, fail-closed illegal transitions, presenter rendering
  projection (10 states), redacted canonical output envelope, bounded retry
  policy (1..=30s)
- crates/focusa-license/src/activation_facade.rs (new): typed facade
  operations (11) mirroring the frozen call stack (id/method/path/mutation/
  success states/failure codes) and typed facade errors (all 33 registry
  codes with http_status/retryable/safe_next_action), typed request context
  with 18 forbidden caller fields, mask_email
- crates/focusa-license/src/activation_client.rs (new): shared activation
  client/session — email input, verification, offers, checkout URL, bounded
  poll, existing key, limited access (Spec 172 overlay), terminal envelope,
  node, lease, refresh, resume, cancellation, recovery — one state machine,
  append-audited ledger, persistable snapshot, transport trait
- crates/focusa-license/src/lib.rs: module wiring + re-exports only
- crates/focusa-license/tests/fixtures/
  spec152e-activation-transcript-fixtures.v1.json (new): deterministic
  transcript fixtures (exact frozen machine encoding, presenter map,
  8 positive transcripts, 9 negative transcripts, 33 error cases)
- crates/focusa-license/tests/spec152e_activation_contract.rs (new): Rust
  replay of the fixtures against the reducer
- tests/spec152e_activation_client_contract_test.py (new): exact
  verification binding the fixtures and Rust surfaces to the frozen
  contracts

Implementation commit: `87c1d5db` (shared activation client/reducer +
typed facade operations/errors + deterministic transcript fixtures +
Rust/Python contract replay)

Bounded result
- crates/focusa-license/src/activation_reducer.rs: `reduce_activation` is the
  single source of truth for the frozen machine — exactly the 48 contract
  `(state, event)` pairs succeed and every other pair fails closed
  (`IllegalTransition`); `recovery_only` accepts nothing (recovery never
  re-grants); no promotion, entitlement, node, or lease can occur before
  mailbox control; paid registrations are never downgraded to limited access
  once a paid journey/entitlement exists. `presenter_state` is a pure
  rendering projection to the 10 frozen presenter states; the
  `ActivationOutputEnvelope` has no field for any forbidden value and
  rejects unmasked email at construction. Retry posture is bounded to the
  frozen 1..=30s window.
- crates/focusa-license/src/activation_facade.rs: all 11 operations match the
  frozen call stack (id, POST/GET, path, mutation flag, success presenter
  states, failure codes); all 33 error codes match the registry
  (http_status, retryable, safe_next_action) with unique canonical labels;
  `ActivationRequestContext` carries no forbidden caller field;
  `mask_email` produces only `^[^@]*\*[^@]*@[^@]+$` masks (c***@example.com)
  and fails closed on unmaskable input.
- crates/focusa-license/src/activation_client.rs: one session drives email
  input → verification → offers → select-offer → checkout → bounded poll →
  terminal envelope → device → lease → delivered, plus existing-key and
  limited-access (Spec 172 overlay) journeys, refresh (refund/revoke settle
  to recovery_only), resume within the poll budget, cancellation
  (denied → recovery_only), and authority-error envelopes carrying the typed
  code + safe next action; every transition is append-audited in a
  deterministic ledger; snapshots persist state, masked email, and budget —
  never poll credentials, raw email, or keys.
- Deterministic transcript fixtures: the fixture state machine is byte-exact
  with the frozen contract (48 transitions, 19 states, 10 presenter states);
  8 positive transcripts (paid terminal Focusa, existing key, limited access,
  refund/revocation/supersession/expiry recovery) replay deterministically;
  9 negative transcripts fail closed (no unverified promotion, no entitlement
  before verification, no node/lease without verified identity, recovery
  never re-grants, no downgrade to limited access, no backward transitions);
  all 33 error cases bind http_status/retryable/safe_next_action and retry
  posture exactly per the frozen retry rules (ENTITLEMENT_FEATURE_REQUIRED /
  ENTITLEMENT_LIMIT_EXHAUSTED are runtime entitlement codes outside the
  activation call stack, asserted as such).

Exact verification (exit codes)
1. python3 tests/spec152e_activation_client_contract_test.py — exit 0 (run
   three times; deterministic, byte-identical output, sha256
   eb248bd103cb8e57cfe99a523acd3815e7afefa31af02d05b9fe64a8be57de66).
   Bounded result:
   {"schema":"focusa.spec152e.activation_client_contract.v1",
   "positive_checks":548,"negative_checks":15,
   "states":19,"transitions":48,"presenter_states":10,"error_codes":33,
   "positive_transcripts_replayed":8,
   "negative_transcripts_replayed":9,
   "rust_surfaces":["activation_reducer","activation_facade",
   "activation_client","spec152e_activation_contract"],
   "result":"passed_fail_closed"}
2. cargo test -p focusa-license — exit 0. lib: 75 passed, 0 failed
   (including 19 new activation unit tests); tests/spec152e_activation_contract.rs:
   5 passed, 0 failed (fixture replay: exact 48-transition machine match,
   presenter map match, positive transcripts, negative fail-closed, error
   cases); existing suites green: authority_lease 11, entitlement_policy_grid
   8, spec152_capsule_manifest 3, spec152f_automation_entitlement 34,
   spec152f_export_entitlement 29, spec152f_release_proof_entitlement 32,
   spec152f_team_remote_entitlement 42, spec172_dynamic_operation_manifest 12,
   spec172_family_inheritance 12. Total 263 passed, 0 failed.

Regression checks (exit codes)
- python3 tests/spec152e_api_contract_test.py — exit 0 (generated activation
  contracts are current; 11 operations, 33 errors).
- python3 tests/spec152e_state_machine_contract_test.py — exit 0 (19 states,
  48 transitions).
- python3 tests/spec152e_product_registry_contract_test.py — exit 0.
- python3 tests/spec152e_facade_registry_contract_test.py — exit 0.
- python3 tests/spec152e_delivery_lease_acceptance_test.py — exit 0
  (prior atom's 23-row §23 matrix unchanged).
- python3 tests/spec152e_recovery_only_surface_test.py — exit 0.

Tests added
- tests/spec152e_activation_client_contract_test.py (exact verification;
  548 positive / 15 negative checks): fixture machine byte-equals the frozen
  machine; every state/error handled exactly once in the shared encoding;
  operation settle projections stay inside the frozen success-state sets;
  positive transcripts replay deterministically; negative transcripts fail
  closed; 33 error cases bind the registry and retry rules; canonical
  envelope required/optional/forbidden fields per the public OpenAPI;
  static Rust surface checks (reducer match encodes every frozen event,
  fail-closed default arm, error labels unique, envelope/request-context have
  no forbidden fields); hygiene scans for unmasked real email, secret
  prefixes, full license keys, and private-key material.
- crates/focusa-license/tests/spec152e_activation_contract.rs (5 tests):
  Rust replay of the same fixtures against `reduce_activation` and
  `presenter_state` — byte-exact machine equivalence, presenter map
  equivalence, deterministic positive replay, negative fail-closed,
  typed error values.
- focusa-license unit tests (19): reducer exact frozen-machine table and
  fail-closed negatives; recovery_only never accepts; no unverified
  promotion/local issuance; paid-never-downgraded; presenter mapping covers
  all states with all 10 presenter states reachable; envelope never carries
  raw email and error codes are typed; retry window bound; operation
  id/method/path/mutation table; 33 unique error codes with registry values;
  request-context validation and forbidden fields; mask_email fail-closed;
  error serde round trip; paid terminal journey end-to-end with redaction;
  bounded poll budget and terminal stop; cancel → recovery_only;
  refund refresh → recovery_only; illegal authority transition fails closed
  without state change; authority error envelope with typed code/retry;
  unmaskable email fails closed before any authority call.

Acceptance criteria
- Every state/error is handled once in shared code; presenters contain
  rendering only and secrets are redacted: verified — the reducer owns all
  transitions; presenter projection is pure; every envelope carries masked
  email only and no forbidden field by construction; fixture and Rust replay
  agree byte-for-byte with the frozen machine; all 33 codes bound once.
- All named positive and negative checks pass (548/15 python + 263 cargo;
  both exit 0, byte-identical across runs); diff contains only the named
  surfaces (the 6 crate files + verification test + this evidence record);
  generated contracts are current (api_contract_test.py and all regression
  checks exit 0); secrets and unmasked real email are absent (hygiene scans
  pass; only reserved @example.com-style fixture input masked to
  c***@example.com in Rust tests, never in artifacts); evidence is replayable
  from the pinned commit (offline, deterministic, byte-identical output,
  sha256 eb248bd1...; Rust fixture replay in the same commit).

Rollback
- Revert only this atom's commits: the implementation commit `87c1d5db` and
  the evidence commit containing this file. Preserve EDD customer/order/
  refund truth, verified identities, licenses, nodes, sequences,
  credentials, Workpoints, Evidence, and migration journals; no authority,
  EDD, node, or lease state is mutated by any of these build-independent
  replay surfaces.

No push, deploy, release, merge, or Beads mutation was performed.
