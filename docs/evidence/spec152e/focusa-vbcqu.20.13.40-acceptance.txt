Spec 152E verified shell delegation — acceptance evidence
Work item: focusa-vbcqu.20.13.40
Title: 152E.05.02 Replace Unix shell licensing with verified delegation
Scope: scripts/install-focusa.sh (canonical Unix installer); argument/help/
telemetry; Rust handoff
Spec authority: docs/152e-edd-centered-universal-multi-surface-licensing-and-
branded-facade-addendum.md (§4 presenter topology, §12 Evaluation journey,
§19 security/privacy, §21 surface consolidation, §22.3 cutover item 8);
Specs 152, 150A, 152A-D. Spec 158 implementation is excluded.

Implementation commit: `6099f6d1` (feat(spec152e): replace Unix shell
licensing with verified delegation)

Exact surfaces (this atom only)
- scripts/install-focusa.sh (modified): help/argument telemetry and Rust
  handoff alignment with the shared activation client
- tests/spec152e_installer_shell_static_test.sh (new): exact verification
  binding the positive and negative verified-delegation checks

Bounded result
- No shell branch can issue Evaluation or entitlement: the bootstrapper
  contains no local validation, no license/authority/receipt JSON issuance,
  no self-Eval or installer grace license, and no raw key/email storage or
  logging. Raw credentials (`--license-key`, `--email`, `--registry`) are
  rejected at parse time with `E_AUTHORITY_RAW_KEY_FORBIDDEN` before any
  state exists.
- Activation intent is collected or forwarded, never decided: `--eval`
  (and `--accept-license`) forward intent to the shared activation client in
  the Rust installer; help text, dry-run telemetry, and header comments now
  state explicitly that Evaluation is authority-issued only and never local
  (`evaluation: authority-issued only; intent forwarded to the shared
  activation client` appears in the dry-run plan when `--eval` is set).
- Official and raw download paths reach the shared client with safe args:
  both the official GitHub release download branch and the raw
  `--release-base-url` mirror branch resolve through the one
  `release_asset_url` resolver, verify SHA256SUMS (+ Cosign fail-closed on
  stable), and converge on exactly one handoff
  (`if "$BOOTSTRAP_BIN" "${ARGS[@]}"; then` occurs once) with allowlisted
  flags only — no caller-controlled product/price/grant/feature/limits input
  exists anywhere in the surface.
- Artifact verification, rollback, repair, export, and uninstall are
  preserved: SHA256SUMS + Cosign verification, `restore_bootstrap_stash`
  rollback, bounded `trap cleanup EXIT INT TERM`, and preserve-by-default
  delegated uninstall (`uninstall_args+=(--keep-data)`) are intact; repair
  and export remain Rust CLI surfaces reached through the same preserved
  binary delegation.

Exact verification (exit codes; byte-identical across runs)
1. `bash tests/spec152e_installer_shell_static_test.sh` — exit 0 (two runs,
   sha256 of output 5a5b309e4105f421b8fd43104c8be638a6e3e7512ca36161fb7e4fdc58494701).
   Named checks (10 positive/negative PASS lines):
   - bash syntax OK
   - no local validation, JSON issuance, self-Eval, or raw key/email storage
   - activation intent delegated to shared client; rollback/uninstall preserved
   - official and raw download paths converge on one shared-client handoff
   - handoff args are allowlisted; no caller-controlled grants/price/features
   - no unmasked email, license keys, or secret material in the surface
   - unknown option fails closed (exit 64) without mutation
   - raw license key rejected without echo or mutation
   - raw email rejected without echo
   - --eval intent disclosed as authority-issued; dry-run mutates nothing
2. `bash tests/spec_focusa_112_shell_ps1_slim_static_test.sh` — exit 0 (two
   runs, sha256 of output 9448614311be0647da0c229178f8a745cf4d105a2b682287fff9bd0d7688e599).
   All 16 named Bash+PowerShell bootstrapper contract checks pass, including
   line-count bound (248 lines), verified Rust handoff, signed authority
   delegation, no systemd/launchd/sc.exe rendering, SHA256SUMS verification,
   Spec 112 citation, and invalid-option fail-closed behavior.

Regression checks (exit codes)
- `bash tests/spec152_installer_shell_static_test.sh` — exit 0 (Spec 152
  shell authority contract unchanged).
- `python3 tests/spec152_installer_presenter_parity_test.py` — exit 0
  (Bash/PowerShell/Rust/menubar presenter parity incl. raw-credential
  rejection and interruption rollback).
- `python3 tests/spec152e_api_contract_test.py` — exit 0 (generated
  activation contracts current).
- `python3 tests/spec152e_state_machine_contract_test.py` — exit 0
  (19 states / 48 transitions unchanged).

Tests added
- tests/spec152e_installer_shell_static_test.sh: exact verification —
  static forbids for local validation/JSON issuance/self-Eval/raw key/email/
  unmasked secret material; static requires for raw-credential rejection,
  single shared-client handoff, official + raw download resolvers,
  rollback/uninstall preservation, and allowlisted handoff args; behavioral
  checks (executable, not source markers): unknown option exits 64 without
  mutation, raw `--license-key`/`--email` rejected before state without echo
  of the secret, `--dry-run --eval` discloses authority-issued Evaluation and
  creates no `.focusa` state.

Acceptance criteria
- No shell branch can issue Evaluation or entitlement: verified statically
  (no issuance/self-Eval/storage patterns) and behaviorally (dry-run --eval
  mutates nothing; raw credentials rejected before state).
- Official and raw download paths reach the shared client with safe args:
  verified — one resolver, both branches, one fail-closed handoff, allowlisted
  flags only.
- All named positive and negative checks pass (10/10 spec152e + 16/16 spec112,
  both exit 0 and byte-identical across runs); diff contains only the named
  surfaces (scripts/install-focusa.sh + this test + this evidence record);
  generated contracts are current (api_contract_test.py and state-machine
  regression exit 0); secrets and unmasked real email are absent (hygiene
  scan forbids email/private-key/secret/token patterns; runtime rejection
  proves no secret echo); evidence is replayable from the pinned commit
  (offline, deterministic, byte-identical output with pinned sha256 hashes).
- Deployed installer: the canonical script remains byte-synchronizable to
  install.focusa.dev/installers/install-focusa.sh via
  scripts/sync-install-bootstrapper.sh; live deployment sync is operator-side
  and was not performed (publication forbidden for this atom).

Rollback
- Revert only this atom's commits: the implementation commit `6099f6d1` and
  the evidence commit containing this file. Preserve EDD customer/order/
  refund truth, verified identities, licenses, nodes, sequences,
  credentials, Workpoints, Evidence, and migration journals; no authority,
  EDD, node, lease, or local install state is mutated by these
  build-independent shell surfaces.

No push, deploy, release, merge, or Beads mutation was performed.
