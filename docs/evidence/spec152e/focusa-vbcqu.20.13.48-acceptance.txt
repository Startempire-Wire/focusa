Spec 152E.05.10 presenter parity, privacy, and install-channel neutrality —
acceptance evidence
Work item: focusa-vbcqu.20.13.48
Title: 152E.05.10 Prove presenter parity, privacy, and install-channel
neutrality
Spec authority: docs/152e-edd-centered-universal-multi-surface-licensing-and-
branded-facade-addendum.md (Spec 152E §4 presenters, §8 product/grant
registry, §12 Evaluation, §14 agent/JSON, §16 dual delivery, §19
security/privacy, §20 stable failures, §21 surface consolidation, §22.3
cutover, §23 acceptance matrix); docs/172-focusa-spec152-license-type-and-
surface-entitlement-governance-addendum.md (Spec 172 overlay:
verified_no_license replaces Evaluation); frozen contracts
docs/contracts/spec152e-activation-internal.v1.json,
spec152e-activation-errors.v1.json, spec152e-agent-activation.v1.json, and
the frozen transcript fixture
crates/focusa-license/tests/fixtures/
spec152e-activation-transcript-fixtures.v1.json; Specs 152, 150A, 152A-D;
Spec 158 implementation excluded. Locked-release correction atom in the
spec152f/spec172 async pipeline.

Exact surfaces (this atom only)
- Generated parity matrix and build-independent gate:
  docs/contracts/spec152e-presenter-parity-matrix.v1.json (new, generated
  contract) and tests/spec152e_presenter_parity_test.py (new, exact
  verification).
- The matrix binds the exact Spec 152E presenter set and their transcripts
  and receipt schemas: Unix (scripts/install-focusa.sh), PowerShell
  (scripts/install-focusa.ps1), Rust installer
  (crates/focusa-cli/src/commands/install.rs), interactive CLI
  (crates/focusa-cli/src/commands/activation_flow.rs + license.rs), agent
  JSON (crates/focusa-license/src/activation_agent.rs +
  apps/pi-extension/src/activation-envelope.ts), menubar
  (apps/menubar/src/lib/activationPresenter.ts), TUI
  (crates/focusa-tui/src/activation_presenter.rs), daemon REST
  (crates/focusa-api/src/routes/license.rs), lifecycle receipts
  (crates/focusa-core/src/install_lifecycle/receipts.rs), source build
  (crates/focusa-license/src/authority_store.rs), UIAI
  (crates/focusa-license/src/uiai_activation.rs), and bundle
  (crates/focusa-license/src/bundle_activation.rs).
- No implementation file of any presenter was modified; this atom only
  proves parity/privacy/channel-neutrality across the committed surfaces.

Exact surfaces (files)
- docs/contracts/spec152e-presenter-parity-matrix.v1.json (new, generated)
- tests/spec152e_presenter_parity_test.py (new, exact verification)
- docs/evidence/spec152e/focusa-vbcqu.20.13.48-acceptance.txt (this record)

Implementation commit: `8f9803ce` (feat(spec152e): prove presenter parity,
privacy, and install-channel neutrality)

Bounded result
- Generated the Spec 152E presenter parity matrix contract
  (schema focusa.spec152e.presenter_parity_matrix.v1): it enumerates the
  exact 12-presenter surface set with per-presenter source paths, roles,
  frozen presenter-state/next-action/error coverage (recomputed by the
  gate), handoff/delegation markers, receipt schemas, credential handling,
  and channel posture; the frozen vocabulary (10 presenter states, 3
  terminal states, 31 next actions, 33 authority error codes, 9 bootstrap
  E_ codes) equals the internal contract, error registry, and agent
  contract; the 8 positive vectors (paid_terminal_focusa,
  existing_key_journey, limited_access_spec172_overlay, refund/revocation/
  supersession/checkout-expiry/verification-expiry recovery) and 9 negative
  vectors equal the frozen transcript fixture.
- Added the build-independent gate that proves: (1) the matrix is CURRENT —
  per-presenter coverage is recomputed from the pinned sources with the
  same quoted-literal algorithm and must equal the committed entries, so
  any future surface drift fails the gate; (2) SAME POSITIVE VECTORS on
  every presenter — every positive vector replays to the same terminal
  presenter state (activated/recovery_only) from one frozen fixture, live
  rendering surfaces (CLI, agent, menubar, TUI, REST) render every journey
  state and the shared next-action table, lifecycle receipts render the
  terminal posture, and delegates (Unix, PowerShell, Rust installer, source
  build, UIAI, bundle) forward the identical journey through exactly one
  fail-closed handoff to the shared activation client; (3) SAME NEGATIVE
  VECTORS — no presenter promotes an unverified email, issues
  local/self-issued entitlement/checkout/node/lease, recovery states never
  regrant or reissue, and paid accounts are never downgraded to limited
  access; (4) PRIVACY — no unmasked real email (only deterministic
  example-domain fixtures and the product-owned public support contact
  support@focusa.dev), no private key/sk-/ghp_ secret material, no
  forbidden output fields (raw email, full key, credentials, card data) in
  any production presenter surface, masked-identity-only, and full-key
  reveal requires BOTH the reveal_key opt-in and reveal_confirmation; (5)
  INSTALL-CHANNEL NEUTRALITY — install_channel is recorded as advisory
  telemetry on activation_client/activation_facade/activation_http and the
  CLI flow values (source_build, official_installer) only; it is never read
  by the reducer, entitlement policy, authority-store state resolution,
  daemon REST gate, install lifecycle, or any rendering surface (test-only
  and #[cfg(test)] occurrences are excluded, matching the source-build
  proof state_resolution_never_reads_install_channel_or_installer_files).
- Receipt schemas bound: focusa.agent_activation_envelope.v1,
  focusa.presenter_entitlement_posture.v1,
  focusa.lifecycle_receipt_presenter_posture.v1,
  focusa.lifecycle_acceptance_receipt.v1, focusa.activation_flow_outcome.v1,
  focusa.windows_verified_bootstrap_plan.v1, and the UIAI/bundle/source
  product contracts — none carry raw credentials, unmasked email, or card
  data.
- No unverified-email promotion, no local/self-issued entitlement, no
  client-controlled EDD price/grants, no independent facade authority, no
  secret or unmasked real-email evidence, no unrelated release work, and no
  publication. No implementation file of any presenter was changed. Cargo/
  release builds remain deferred until the operator's 50% gate; this gate
  is build-independent by design.

Exact verification (exit codes)
1. `python3 tests/spec152e_presenter_parity_test.py` — exit 0.
   Spec 152E presenter parity/privacy/channel-neutrality gate passed
   (positive=331, negative=294): matrix currency across all 12 presenters,
   shared positive/negative vector equivalence, privacy scans, and
   install-channel neutrality.

Regression checks (exit codes)
- `python3 tests/spec152e_agent_json_contract_test.py` — exit 0.
- `python3 tests/spec152e_cli_activation_test.py` — exit 0.
- `python3 tests/spec152e_tui_rest_activation_test.py` — exit 0.
- `python3 tests/spec152e_state_machine_contract_test.py` — exit 0.
- `python3 tests/spec152e_source_build_activation_test.py` — exit 0.
- `python3 -m py_compile tests/spec152e_presenter_parity_test.py` — exit 0.
- JSON parse of the generated matrix (loaded by the gate itself) — valid.

Tests added
- tests/spec152e_presenter_parity_test.py (331 positive / 294 negative
  assertions), the build-independent gate for the generated parity matrix;
  no Rust test was added because no Rust surface was modified.

Rollback
- Revert only this atom's commits (implementation commit 8f9803ce above
  plus the evidence commit containing this file). Preserve EDD
  customer/order/refund truth, verified identities, licenses, nodes,
  sequences, credentials, Workpoints, Evidence, and migration journals; no
  authority, EDD, node, lease, or local install state is mutated by these
  parity proofs and replay tests.

No push, deploy, release, merge, or Beads mutation was performed.
