Spec 152E.05.07 raw binary / local source-build first-run activation —
acceptance evidence
Work item: focusa-vbcqu.20.13.45
Title: 152E.05.07 Prove raw binary and local source-build activation
Spec authority: docs/152e-edd-centered-universal-multi-surface-licensing-and-
branded-facade-addendum.md (§14.3 source and unofficial builds, §24.9 source
builds and agents use the same flow); frozen contracts
docs/contracts/spec152e-source-build-first-run-fixture.v1.json (new, this
atom) and spec152e-activation-internal.v1.json; Specs 152, 150A, 152A-D;
Spec 172 overlay; Spec 158 implementation excluded. Locked-release
correction atom in the spec152f/spec172 async pipeline.

Exact surfaces (this atom only)
- Source-build run path: crates/focusa-cli/src/commands/activation_flow.rs —
  the CLI presenter identity `CLI_FLOW` (presenter=cli,
  install_channel=source_build) drives the SAME shared ActivationSession as
  the installer; license.rs `run_activation_flow_command` /
  `run_agent_activation_command` wire it; plaintext activation is retired
  (E_AUTHORITY_COMMAND_RETIRED).
- Direct binary first protected action: crates/focusa-core/src/license.rs
  `require_base_product` / `LicenseError::BaseProductRequired` (fails closed
  until a signed lease exists) and crates/focusa-api/src/middleware/
  entitlement.rs `ENTITLEMENT_BASE_REQUIRED` for value-producing mutations.
- Config/credential discovery: crates/focusa-license/src/authority_credentials.rs
  (`load_or_create_node_identity`, protected credential store),
  activation_flow.rs `resolve_flow_node_identity`, authority_store.rs
  `resolve_authority_state` (missing durable state → unactivated, corrupt
  state → recovery_only, embedded production trust roots only).
- Activation CLI/API: `focusa license activate-flow` (interactive and
  --agent), daemon REST `/v1/license/status` + `/v1/activation/status`.

Exact surfaces (files)
- crates/focusa-license/src/authority_store.rs (3 new unit tests)
- docs/contracts/spec152e-source-build-first-run-fixture.v1.json (new
  frozen fixture)
- tests/spec152e_source_build_activation_test.py (new, exact verification)
- docs/evidence/spec152e/focusa-vbcqu.20.13.45-acceptance.txt (this record)

Implementation commit: `be46dd36` (feat(spec152e): prove raw binary and
local source-build activation)

Bounded result
- Added a frozen source-build/raw-binary first-run fixture
  (spec152e-source-build-first-run-fixture.v1.json): models a fresh
  `$HOME/.config/focusa` config directory with no installer provenance
  (no receipts, no install root, no service unit, no PATH edits) and a
  direct-binary invocation (`cargo run` / `./target/debug/focusa` /
  manually copied binary). The fixture pins `install_channel=source_build`
  as advisory telemetry, the expected first-run status projection
  (unactivated → presenter_state email_required), the protected-first-action
  gate (BaseProductRequired / ENTITLEMENT_BASE_REQUIRED), the same universal
  authority flow (email → verify → offer → checkout/poll → signed lease;
  device-code for noninteractive; --agent typed envelopes with resumable
  handle), the non-grant matrix (8 rows: missing installer files, missing
  lease, either channel value, installer markers, legacy plaintext license,
  corrupt lease, missing trust roots — none grants), and the deletion matrix
  (4 rows: deleting installer state changes nothing, deleting the signed
  lease locks to unactivated, etc.).
- Added 3 unit tests in authority_store.rs proving §14.3 with the frozen
  Spec 152 golden vector:
  source_build_first_run_without_installer_or_lease_grants_nothing (fresh
  dir, noise files only → Unactivated with authority_lease_missing, zero
  feature/limit grants, no install_channel grant surface in the projection);
  deleting_installer_state_never_unlocks_and_deleting_the_lease_locks (with
  a valid signed lease the machine is Active regardless of installer
  provenance; deleting installer artifacts changes nothing; deleting the
  lease itself locks to Unactivated — never a local/self-issued fallback);
  state_resolution_never_reads_install_channel_or_installer_files
  (installer markers on disk are never consulted; the decision has no
  channel input).
- Added the first-run Python test (tests/spec152e_source_build_activation_test.py)
  binding the fixture to the exact surfaces: CLI_FLOW source_build identity
  driving ActivationSession::begin/resume with no presenter-side transition
  table; direct-binary base gate (require_base_product /
  BaseProductRequired, ENTITLEMENT_BASE_REQUIRED, unactivated→email_required);
  config/credential discovery (~/.config/focusa, node identity, protected
  store, embedded trust roots with test/local markers forbidden);
  fail-closed installer pins (E_AUTHORITY_EXISTING_UNUSABLE,
  E_AUTHORITY_RAW_KEY_FORBIDDEN, E_AUTHORITY_LEASE_UNUSABLE,
  E_AUTHORITY_DEVICE_DENIED, E_AUTHORITY_ACTIVATION_UNSETTLED; no
  persist_eval_license anywhere); install_channel advisory-only (recorded in
  registration/context/HTTP payload, never read by the reducer, authority
  store, entitlement policy, core base gate, or daemon middleware; decision
  surfaces never read installer state either); the paid / Evaluation
  (limited-access overlay) / existing-key journeys reaching activated through
  the shared flow (bound to the Rust transcript replay tests in the same
  commit); hygiene (no raw email, key, poll credential, or card field on any
  surface or in the fixture).
- No unverified-email promotion, no local/self-issued entitlement, no
  client-controlled EDD price/grants, no independent facade authority, no
  raw or unmasked real-email evidence anywhere. Cargo/release builds remain
  deferred until the operator's 50% gate.

Exact verification (exit codes)
1. `python3 tests/spec152e_source_build_activation_test.py` — exit 0.
   positive=66, negative=40. Binds the fixture + frozen internal contract to
   activation_flow.rs, license.rs, install.rs, authority_store.rs,
   activation_client.rs, activation_facade.rs, activation_http.rs,
   activation_reducer.rs, authority_credentials.rs, entitlement_policy.rs,
   focusa-core license.rs, daemon middleware/entitlement.rs, and REST
   license routes; asserts channel-neutral decisions, the non-grant and
   deletion matrices, the first protected action fail-closed gates, and
   surface hygiene.
2. `cargo test -p focusa-license authority_store` — exit 0.
   test result: ok. 3 passed; 0 failed (89 filtered out) — the three new
   unit tests above.

Regression checks (exit codes)
- `cargo test -p focusa-license` — exit 0: 92 unit + 188 integration tests
  passed (authority_lease 11, entitlement_policy_grid 8, capsule_manifest 3,
  spec152e_activation_contract 5, spec152f_automation 34, spec152f_export 29,
  spec152f_release_proof 32, spec152f_team_remote 42, spec172_dynamic 12,
  spec172_family_inheritance 12); 0 failed.
- `cargo test -p focusa-cli activation_flow` — exit 0: 18 passed; 0 failed
  (paid terminal, existing key, limited-access overlay, recovery, resume,
  agent, persistence replay tests through the shared ActivationSession).
- `python3 tests/spec152e_cli_activation_test.py` — exit 0.
- `python3 tests/spec152e_installer_activation_test.py` — exit 0.
- `python3 tests/spec152e_tui_rest_activation_test.py` — exit 0.
- `python3 tests/spec152e_agent_json_contract_test.py` — exit 0.
- `python3 scripts/generate-spec152e-activation-contracts.py --check` —
  exit 0: "Spec 152E activation contracts are current".
- `rustfmt --edition 2024 --check` on the changed Rust file (nightly
  toolchain rustfmt) — clean, 0 diffs.

Tests added
- focusa-license authority_store:
  source_build_first_run_without_installer_or_lease_grants_nothing,
  deleting_installer_state_never_unlocks_and_deleting_the_lease_locks,
  state_resolution_never_reads_install_channel_or_installer_files.
- tests/spec152e_source_build_activation_test.py (66 positive / 40 negative).

Rollback
- Revert only this atom's commits (implementation commit above plus the
  evidence commit containing this file). Preserve EDD customer/order/refund
  truth, verified identities, licenses, nodes, sequences, credentials,
  Workpoints, Evidence, and migration journals; no authority, EDD, node,
  lease, or local install state is mutated by these first-run proofs and
  replay tests.

No push, deploy, release, merge, or Beads mutation was performed.
