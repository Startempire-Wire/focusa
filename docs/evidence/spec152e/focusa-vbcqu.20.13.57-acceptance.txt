Spec 152E acceptance record
Work item: focusa-vbcqu.20.13.57
Title: 152E.07.03 Prove terminal and agent paid activation
Implementation commit: `ca91d01d` (journey contract + pinned fixture + e2e
gate)
Spec authority: docs/152e-edd-centered-universal-multi-surface-licensing-and-
branded-facade-addendum.md (spec 152E §14 terminal/agent/source-build
experience, §15 human key and signed lease separation, §16 dual-channel
license delivery, §18 refund/revoke/recovery, §23 acceptance-matrix rows
"Terminal paid Focusa" and "Agent paid Focusa"); Specs 152, 150A, 152A-D;
Spec 158 implementation excluded. Locked-release correction atom in the
spec152f/spec172 async pipeline. Cargo/release builds remain deferred until
the operator's 50% gate.

Exact surfaces (this atom only)
- docs/contracts/spec152e-terminal-agent-paid-activation.v1.php — the
  terminal + agent paid-activation journey authority kernel: interactive CLI
  and agent JSON presenters (startRegistration with presenter/install_channel/
  device key, verifyEmail single-use attempt-bounded challenge, promote),
  checkout link (createCheckoutIntent, server-owned facade origin + path +
  opaque token), bounded poll (poll/max_polls=40, exhaustion cancels to
  recovery_only), EDD order/key (completePayment with checkout-email
  integrity, issueLicense), dual key delivery (prepareTerminalDelivery:
  transactional email + one-time device-encrypted terminal envelope using the
  canonical X25519/HKDF/AES-256-GCM crypto from docs/contracts/spec152e-
  terminal-delivery-envelope.v1.php), pause/resume and agentStatus (agent-only
  lifecycle with protected poll credential; terminal registrations refuse
  resume steps), credential store + explicit key reveal (credentialStore,
  revealKey with opt-in AND confirmation, one-time, envelope-lifetime-bound),
  node registration, signed device-bound lease (via the canonical Ed25519
  key-set seam from docs/contracts/spec152e-edd-bound-lease-issuer.v1.php),
  and EDD refund cleanup (refund) that increments the monotonic sequence,
  revokes the lease, denies refresh, and preserves truth.
- docs/contracts/spec152e-terminal-agent-paid-activation-fixture.v1.json —
  pinned public-synthetic non-production journey vectors: two paid sessions
  (cli_terminal and agent_json) plus one abandoned agent session, verified
  identity digests, one-time verification codes, server-owned product/price
  mapping, digest-derived EDD customer/order/license ids, X25519 device
  keys (RFC 7748 X25519(priv, u=9) derivation, cross-verified against
  libsodium crypto_scalarmult), authority lease keys, envelope crypto test
  seams, bounded-poll policy, journey state machines, expectations, and
  journal row vectors.
- tests/spec152e_paid_terminal_agent_e2e_test.py — the exact verification
  gate: deterministic sqlite PHP harness executed twice with byte-identical
  output, independent Ed25519 lease-signature verification with
  `cryptography`, independent X25519+HKDF+AES-256-GCM decryption of both
  one-time terminal envelopes proving the same canonical EDD key on the email
  and terminal channels, masked agent-transcript assertions, and static
  fixture/contract/redaction invariants.
- Evidence: docs/evidence/spec152e/focusa-vbcqu.20.13.57-acceptance.txt.

Bounded result
- Terminal session (cli_terminal): pending registration attempt -> single-use
  483921 mailbox verification -> account + EDD customer 1494 promotion ->
  server-owned checkout link on https://install.focusa.dev/activate/checkout/
  (download 4601 / price 46011 / 697.00) -> bounded poll while payment
  pending -> test-mode EDD order 9081 -> EDD Software Licensing human key
  (derived from license 7613 / order 9081, masked ********-********-********-
  2D90) -> dual key delivery: transactional email + one-time device-encrypted
  terminal envelope (X25519/HKDF/AES-256-GCM, fixed test seams) -> terminal
  poll returns the envelope exactly once (second poll fails closed) -> device
  opens the envelope and stores the key in the protected credential store ->
  explicit customer-controlled reveal (opt-in + confirmation) returns the
  canonical key once (replay denied) -> node registration -> signed
  device-bound lease (sequence 1, paid posture, authority-lease-2026-01,
  Ed25519 over FOCUSA-AUTHORITY-LEASE-V1) -> EDD refund cleanup: sequence
  increments to 2, order/license/lease flip to refunded, refresh denied with
  recovery_only posture, and account/order/delivery/node/evidence rows
  preserved.
- Agent session (agent_json): structured challenge -> human verification ->
  masked agent transcript (focusa.agent_activation_envelope.v1) with typed
  human-action states (email_verification_pending -> selection_required ->
  checkout_required -> payment_pending -> license_delivery_ready ->
  activated -> recovery_only), safe checkout link, key_present=true /
  key_visible=false, and never an email, code, consent, payment confirmation,
  license, envelope, or credential -> pause rotates the protected poll
  credential; poll while paused is denied; resume with the re-supplied
  credential is a bounded poll -> human payment at the checkout link completes
  order 9796 -> EDD key (license 7440, masked ********-********-********-14D7)
  -> dual delivery -> the agent device opens its one-time envelope out-of-band
  (envelope never in the agent transcript; second consumption denied) ->
  credential store + explicit reveal -> node -> signed lease -> refund
  cleanup (sequence 2, recovery_only).
- Abandoned agent session: reaches payment_pending and polls past the 40-poll
  budget; the session cancels fail-closed to recovery_only with zero
  order/license/node/lease/refund rows.
- Fail-closed proof: unverified email never promotes or creates entitlement;
  verification codes are single-use and attempt-bounded (5); caller-supplied
  product/price/grant/redirect fields fail closed
  (CALLER_CONTROLLED_GRANT_DENIED); facade spoof fails (FACADE_ORIGIN_DENIED);
  unknown presenter/channel/device-key denied; wrong device key denied at
  poll and node registration (DEVICE_BINDING_MISMATCH); a different checkout
  email holds fulfillment (EDD_ORDER_UNVERIFIED) until the verified identity
  matches; poll without/with wrong credential fails; pause/resume/agentStatus
  refused for terminal presenters; terminal states refuse pause/resume;
  double pause denied; reveal without opt-in or without confirmation denied;
  reveal replay denied; expired envelope and expired credential denied; the
  one-time envelope cannot be consumed twice; idempotent replays return the
  stored outcome with zero new rows and idempotency-key reuse with a different
  request fails (IDEMPOTENCY_CONFLICT); the contract contains no destructive
  SQL and no raw-email/raw-key/payment-id storage (beyond the protected
  credential store); spec 158 is excluded.
- Both signed leases are independently re-verified in Python with
  `cryptography` Ed25519PublicKey over the domain-separated payload
  (FOCUSA-AUTHORITY-LEASE-V1), byte-compatible with the Rust verifier; both
  one-time terminal envelopes are independently decrypted with X25519 +
  HKDF-SHA256 + AES-256-GCM (RFC 7748 X25519(priv, u=9) device keys
  cross-verified against libsodium crypto_scalarmult) and the decrypted key
  equals the canonical EDD key that was emailed — same canonical key both
  channels.
- Redacted receipts (focusa.spec152e.terminal_agent_receipt.v1) carry only
  masked email and masked key plus immutable sha256 handles; no raw email,
  raw human key, payment reference, or secret material appears in any
  artifact (contract, fixture, harness output, or evidence).

Exact verification (exit codes)
1. `python3 tests/spec152e_paid_terminal_agent_e2e_test.py` — exit 0, run
   twice on 2026-08-09 with byte-identical harness output (harness sha256
   19dc2fc30560a2b0398f7f47acf74479c516757c0bc6d671577108ec8c807d3a; contract
   sha256 c2f5a4069d74ebc0d65f0ea2a12c55917aec6835cc0c4785884406cf9c857d1c;
   fixture sha256 14a127b039385089ba15a41e0bb9b13e21b6e24fd9ceaf38ebda00d49f8f6358).
   Summary: positive_checks=160 negative_checks=40 journey_states_terminal=11
   journey_states_agent=10 sequences={terminal:2,agent:2}
   receipt_sha256_terminal=ba81d8d4ebbed89ad57f9241cc155f0bff08c8e725b6b4746fe90af0c6339d56
   receipt_sha256_agent=e1a74f074875c6e5946abc5dec69bcd3794e3e4c825032fbe06a064bdcaca511
   lease_signature_verified=true
   terminal_envelope_decrypted_same_canonical_key=true
   agent_transcript_masked=true harness_replay_identical=true result=passed.

Regression checks (exit codes)
- `python3 tests/spec152e_complete_static_gate.py` — exit 0 (the atom
  20.13.55 build-independent gate now discovers 68 test gates, 68/68 passed;
  4/4 generators --check current; php -l on all 39 Spec 152E PHP contracts
  including the new journey contract; JSON parse of 24 Spec 152E JSON
  contracts/fixtures including the new fixture; redaction scan of 67
  contract/fixture/generator sources: zero email literals, zero secrets,
  zero raw-key evidence; deferred Cargo enumeration unchanged at 23 surfaces
  / 148 test markers, none claimed passed).
- `php -l docs/contracts/spec152e-terminal-agent-paid-activation.v1.php` —
  exit 0.
- `git diff` for this atom contains only the three exact-surface files
  (journey contract + fixture + e2e gate) plus this evidence record.
- No cargo command was run: this atom touches no Rust surface and the
  locked-release FORBIDDEN rule defers cargo/release builds to the
  operator's 50% gate.

Tests added
- tests/spec152e_paid_terminal_agent_e2e_test.py — 160 positive / 40 negative
  checks: terminal and agent journey execution on sqlite; pre-flight
  negatives (facade spoof, unmapped product, caller price, unknown presenter/
  channel/device key); verification single-use and attempt-bounded; checkout
  email integrity; caller grant rejection; bounded poll with budget
  exhaustion cancel-to-recovery_only; pause/resume agent-only lifecycle with
  credential rotation; masked agent transcript with forbidden-field scans;
  one-time terminal envelope delivery and consumption; credential store and
  explicit reveal policy; signed-lease Ed25519 verification; X25519 envelope
  decryption proving the same canonical key both channels; redacted receipts
  with immutable handles; pinned row counts and journey states; contract
  static invariants (schema, tables, methods, fail-closed codes, server-owned
  mapping, allowlist, preservation-only, no destructive SQL, no raw email/
  payment storage); artifact redaction scans.

Rollback
- Revert only this atom's commits (the implementation commit containing the
  three exact-surface files and the evidence commit containing this file).
  Preserve EDD customer/order/refund truth, verified identities, licenses,
  nodes, sequences, credentials, Workpoints, Evidence, and migration journals.
  The journey contract is a self-contained synthetic-proof kernel with its own
  sqlite-only tables; no production authority, EDD, node, lease, or local
  install state is touched. The canonical lease signer
  (spec152e-edd-bound-lease-issuer.v1.php) and the canonical terminal envelope
  crypto (spec152e-terminal-delivery-envelope.v1.php) are loaded read-only at
  test runtime and are not modified.

No push, deploy, release, merge, or Beads mutation was performed.
