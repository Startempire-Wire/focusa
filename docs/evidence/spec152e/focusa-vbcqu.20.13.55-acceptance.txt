Spec 152E acceptance record
Work item: focusa-vbcqu.20.13.55
Title: 152E.07.01 Run complete build-independent Spec 152E gate
Implementation commit: `43c35373` (gate + generated-contract corrections)
Spec authority: docs/152e-edd-centered-universal-multi-surface-licensing-and-
branded-facade-addendum.md; Specs 152, 150A, 152A-D; Spec 158 implementation
excluded. Locked-release correction atom in the spec152f/spec172 async
pipeline. Cargo/release builds remain deferred until the operator's 50% gate.

Exact surfaces (this atom only)
- tests/spec152e_complete_static_gate.py — the complete build-independent
  Spec 152E static gate (exact verification; runs every non-release,
  non-Cargo gate and enumerates the exact deferred Cargo gates).
- Generated/current contract corrections surfaced by the gate and fixed so
  no stale contract, secret, or unmasked real email remains:
  - docs/contracts/spec152e-deployed-surface-inventory.v1.json
    (repository_sha256 pins for installer.unix / installer.windows re-pinned
    to the current repository assets; deployed freeze facts and
    parity=diverged untouched)
  - docs/contracts/spec152e-installer-route-manifest.v1.json
    (regenerated by scripts/generate-spec152e-installer-route-manifest.py;
    /focusa and /powershell trust.sha256 now equal the current repo assets)
  - docs/contracts/spec152e-presenter-parity-matrix.v1.json
    (email_scan_note redacted: the product-owned public support contact is
    presenter-help-text only and no email literal remains in any Spec 152E
    contract, fixture, or generator)
- Evidence: docs/evidence/spec152e/focusa-vbcqu.20.13.55-acceptance.txt.

Bounded result
- Ran EVERY non-release, non-Cargo Spec 152E gate available under the
  current operator build deferral: all 66 gates under tests/spec152e_*
  (36 Python, 22 PHP, 3 Node .mjs, 2 shell, 1 PowerShell), dispatched by
  runtime (python3 / php / node / bash / pwsh), plus all four Spec 152E
  generators in --check mode (facade registry, EDD product registry,
  installer route manifest, activation contracts public/internal/errors) —
  66/66 test gates and 4/4 generator gates passed with real exit codes.
- Static lint: php -l on all 37 Spec 152E PHP contracts; bash -n on the
  shell installer surface and Spec 152E shell gates; compile() of every
  Spec 152E Python gate and generator; JSON parse of 22 Spec 152E JSON
  contracts/fixtures; YAML parse of 3 Spec 152E YAML sources.
- Redaction checks: 63 contract/fixture/generator sources and 6 presenter
  surfaces scanned — zero email literals in contracts/fixtures/generators,
  zero secret prefixes, zero synthetic focusa_live key shapes, zero
  private-key material, zero GitHub tokens, zero raw license-shaped
  evidence; presenter surfaces carry only reserved-domain fixtures or the
  single documented product-owned public support contact (help text).
- Stale-contract correction (pre-existing, surfaced by the gate): the
  deployed-surface inventory and installer route manifest pinned
  repository_sha256 values for scripts/install-focusa.sh and
  scripts/install-focusa.ps1 from before the verified-delegation installer
  replacement (commit 6099f6d1), so the generated manifest was stale and
  tests/spec152e_install_facade_routes_test.py failed. Re-pinned both
  repository digests to the current assets (b74e06f3... /
  5e9b1fb7...) and regenerated the manifest; the only manifest delta is the
  two digest pins, and the inventory's deployed freeze facts,
  parity=diverged, and every other row are untouched.
- Redaction correction (pre-existing, surfaced by the gate): the
  presenter-parity matrix embedded the literal support@focusa.dev in its
  email_scan_note, which tests/spec152e_delivery_lease_acceptance_test.py
  rejects (contracts must contain no non-reserved email literal). The note
  now uses the masked support@focusa[DOT]dev form; the product-owned public
  support contact remains allowed ONLY as help text in presenter sources
  (crates/focusa-cli/src/commands/license.rs) and every Spec 152E test that
  previously permitted it still passes.
- Changed-file formatting: every file this atom changes (gate, three
  corrected contracts, evidence record) carries no trailing whitespace,
  tabs, CRLF, or missing final newline; the committed installer route
  manifest is byte-identical to generator output; manifest and inventory
  repository-verified pins agree with the current repository assets.
- Deferred build gates enumerated, NOT run: 23 Cargo surfaces referencing
  Spec 152E are enumerated (cargo_integration_test /
  cargo_unit_test_module / spec152e_surface_reference), totaling 148
  #[test] markers, all with status=deferred_build_gate and
  claimed_passed=false under the operator's 50% build-deferral gate. No
  cargo command was executed; no deferred test is claimed to have passed.
- No unverified-email promotion, no local/self-issued entitlement, no
  independent facade authority, no client-controlled EDD price/grants, and
  no secret or unmasked real-email evidence in any Spec 152E artifact.

Exact verification (exit codes)
1. `python3 tests/spec152e_complete_static_gate.py` — exit 0, run multiple
   times on 2026-08-09 (all exit 0; runs are byte-identical given the same
   pinned tree). Final replayable summary sha256
   d3ce9b9304ef2e387bff6d3ec02bb9074a062858218ea46fa1c8cf402e9725ba.
   Summary:
   schema=focusa.spec152e.complete_static_gate.v1 atom=focusa-vbcqu.20.13.55
   positive_checks=90 negative_checks=296
   test_gates=66 test_gates_passed=66
   generator_gates=4 generator_gates_passed=4
   php_contracts_linted=37 shell_surfaces_linted=3
   json_documents_parsed=22 yaml_sources_parsed=3
   redaction_sources_scanned=63 presenter_sources_scanned=6
   deferred_build_gates=23 deferred_cargo_tests=148 result=passed
   (negative_checks includes the four changed-file formatting checks per
   changed file; the evidence record itself is a changed file in the tree.)
2. Repaired gates that previously failed (all now exit 0):
   - `python3 tests/spec152e_install_facade_routes_test.py` — exit 0
     (stale installer digest pins corrected; 90 positive / 26 negative).
   - `python3 tests/spec152e_delivery_lease_acceptance_test.py` — exit 0
     (parity-matrix email literal redacted).
   - `python3 tests/spec152e_presenter_parity_test.py` — exit 0 (331
     positive / 294 negative; matrix currency and support-contact exception
     intact).

Regression checks (exit codes)
- All 66 Spec 152E gates pass under the complete gate (exit 0 each, listed
  in the gate output); every gate in tests/spec152e_* was executed by the
  complete gate, including the atom 20.13.54 canary gate
  (python3 tests/spec152e_migration_canary_test.py — exit 0,
  harness sha256 1bbf1366... unchanged) and the atom 20.13.53 cutover gate
  (python3 tests/spec152e_authority_cutover_test.py — exit 0).
- `python3 scripts/generate-spec152e-installer-route-manifest.py --check`
  — exit 0 (contracts current after regeneration).
- `python3 scripts/generate-spec152e-facade-registry.py --check`,
  `python3 scripts/generate-spec152e-product-registry.py --check`,
  `python3 scripts/generate-spec152e-activation-contracts.py --check` —
  all exit 0.
- `git diff` for this atom contains only the four exact-surface files (gate
  + three Spec 152E contracts) plus this evidence record.
- No cargo command was run: this atom touches no Rust surface and the
  locked-release FORBIDDEN rule defers cargo/release builds to the
  operator's 50% gate. The 23 deferred Cargo surfaces are enumerated with
  claimed_passed=false.

Tests added
- tests/spec152e_complete_static_gate.py — the exact verification gate
  (90 positive / 292 negative checks): runtime discovery; execution of all
  66 Spec 152E test gates with real exit codes and output hashes; all four
  generators in --check mode; php -l / bash -n / compile() / JSON / YAML
  static lint; redaction scans of contracts, fixtures, generators, and
  presenter surfaces; changed-file formatting and generated-contract
  currency (manifest == generator output, inventory/manifest pins == repo
  assets); and the exact deferred-Cargo enumeration (23 surfaces, 148 test
  markers, none claimed passed).

Rollback
- Revert only this atom's commits (implementation commit plus the evidence
  commit containing this file). Preserve EDD customer/order/refund truth,
  verified identities, licenses, nodes, sequences, credentials, Workpoints,
  Evidence, and migration journals. The three contract corrections are
  pure metadata pins/notes (no authority, EDD, node, lease, or local
  install state is mutated); reverting restores the pre-existing stale
  digests and the pre-existing email literal, which the complete gate would
  again report as stale-contract/redaction failures.

No push, deploy, release, merge, or Beads mutation was performed.
