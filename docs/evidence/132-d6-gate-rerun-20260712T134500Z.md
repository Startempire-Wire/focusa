# Spec 132 D6 gate rerun — 2026-07-12

## Results

```text
spec112_pi_extension_archive_smoke_test.sh                 BLOCKED (host linker permission)
spec128_update_runtime_test.sh                             PASS
spec_install_codesign_verify_static_test.sh                PASS
spec_release_matrix_static_test.sh                         PASS
spec_install_service_rust_static_test.sh                   PASS
spec_focusa_112_uninstall_cmd_static_test.sh                PASS
spec_focusa_117_release_install_postcard_static_test.sh     PASS
release_version_proof_static_test.sh                       PASS
spec_install_rust_static_test.sh                            PASS
spec_install_completion_order_test.sh                       PASS
spec_install_failure_rollback_test.sh                       PASS
spec_install_pi_integration_truth_test.sh                   PASS
spec_install_ui_integration_test.sh                         PASS
```

## Failure classification and correction

- The Pi archive smoke test was stale: it sourced the removed shell `install_pi_extension` helper and was not executable. It now invokes a real Rust unit-test archive fixture (staging, bounded npm setup, and atomic activation) and preserves the Rust static truth guard. The test source is corrected, but this host cannot link Rust tests (`cc: Permission denied`), so executable Pi proof is environment-blocked rather than claimed green.
- The OTA runtime assertion was stale: it expected `apply_allowed=true` and verified trust metadata even when the live release had no checksum/signature assets. It now asserts the implemented fail-closed behavior (`apply_allowed=false`, explicit checksum/signature blockers, and atomic staging proof) and passes. No update was applied. The corrected OTA test passes.
- The release-install postcard guard found a real documentation defect: README lacked the postcard link. The link was added and the guard passes.
- The release-version guard found generated version metadata stale. The canonical generator refreshed the generated summary/stamp and the guard passes.
- Codesign, release matrix, service, uninstall, installer, JSON, and rollback gates passed without weakening behavior.

## Remaining external gate

```text
scripts/verify-bootstrapper-parity.sh  FAIL (live bootstrapper missing at /home/focusadev/install.focusa.dev/public_html/installers/install-focusa.sh)
```

This is an external live-host parity prerequisite, not an implementation failure. Sync/deploy is prohibited by the active Spec 132 release/deploy freeze, so D6 remains open and is not falsely closed. No release or deployment was performed.
