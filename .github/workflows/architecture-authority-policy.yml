name: Architecture Authority Policy

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  authority-policy:
    runs-on: [self-hosted, Linux, X64, ovh-build-2]
    steps:
      - uses: actions/checkout@v4
      - name: Verify architecture authority constitution
        run: bash tests/architecture_authority_policy_static_test.sh
